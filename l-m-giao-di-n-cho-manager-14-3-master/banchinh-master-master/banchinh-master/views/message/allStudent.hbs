
<!DOCTYPE html>
<html>

<head>
   <title></title>
   
</head>
<body>
  
   <table class="table table-striped">
    <thead>
      {{!-- {{#each user}}   --}}
      <tr>
        <th>Username</th>
        <td>{{data.username}}</td>
      </tr>
      <tr>
        <th>Email</th>
         <td>{{data.email}}</td>
      </tr>
      <tr>
        <th>Faculity</th>
         <td>{{data.slug}}</td>
      </tr>
      <tr>
        <th>Phone</th>
         <td>Phone</td>
      </tr>
      <tr>
        <th>Birthday</th>
         <td>Birthday</td>
      </tr>
      <tr>
        <th>Address</th>
         <td>Address</td>
      </tr>

      {{!-- {{/each}} --}}
    </thead>
  </table>
                    <form id="add" action="/message/add_chat/{{data.email}}" method="post" >
                      {{#if isFriend }}
                        <button class="add-friend">Add to Box chat</button>
                      {{/if}}
                    </form>
                            
                     {{#if isMessage }}
                           <form action="/message/send_messageTeacher/{{cookies.email}}/{{data.email}}"
                                    method="GET">
                           <button class="message">Message</button>
                           </form>
                     {{/if}}   
                     

</body>
</html>